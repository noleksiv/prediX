<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoviePrediction.CustomViews.PopupLoading"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
             CloseWhenBackgroundIsClicked="False">
    <Frame          Margin="12"
                    Padding="24"
                    CornerRadius="5"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    BackgroundColor="Transparent">
        <StackLayout>
            <ActivityIndicator IsRunning="True" IsVisible="True" Color="Red"/>
            <Label Text="{Binding Caption}" TextColor="{StaticResource helperWhiteColor}" VerticalOptions="Center" HorizontalOptions="CenterAndExpand" FontSize="14"/>
        </StackLayout>
    </Frame>
</pages:PopupPage>